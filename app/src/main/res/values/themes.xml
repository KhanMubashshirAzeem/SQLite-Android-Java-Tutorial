<resources>
    <!-- Base application theme -->
    <style name="Base.Theme.SQLiteDBGFG" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/black</item>
    </style>

    <style name="Theme.SQLiteDBGFG" parent="Base.Theme.SQLiteDBGFG">
        <item name="android:windowBackground">@color/bg_color</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- Custom Action Bar style -->
    <style name="MyActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="background">@color/black</item>
        <item name="titleTextStyle">@style/MyActionBarTitleText</item>
    </style>

    <!-- Custom Action Bar title text style -->
    <style name="MyActionBarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Custom button style -->
    <style name="CustomMaterialButton" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Existing AppTheme styles -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/bg_color</item>
    </style>

    <!-- Text appearance for TextInputLayout hint text -->
    <style name="TextAppearance.App.TextInputLayout" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Custom TextInputLayout hint style -->
    <style name="TextInputLayoutHintStyle" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/black</item>
    </style>
</resources>
